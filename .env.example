# ── LLM Configuration (Required) ──────────────────────────────────────────
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=4096

# ── Security ─────────────────────────────────────────────────────────────
# Generate both with:  python -m hub.core.security
JWT_SECRET=
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=
PRODUCTION=false

# ── Telegram ─────────────────────────────────────────────────────────────
# Required for Agent Hub webhook replies
TELEGRAM_BOT_TOKEN=

# Secret token Telegram sends in the X-Telegram-Bot-Api-Secret-Token header.
# Set this in your bot via:  curl .../setWebhook?url=...&secret_token=YOUR_TOKEN
TELEGRAM_WEBHOOK_SECRET=

# ── Optional ─────────────────────────────────────────────────────────────
# CV_PHOTO_PATH=photos/photo.jpg
# WEATHER_API_KEY=

# Job search + alerts (Adzuna, free). Get app_id/app_key at developer.adzuna.com.
# Leave unset to disable. Telegram /jobs searches + subscribes you to alerts.
# ADZUNA_APP_ID=
# ADZUNA_APP_KEY=
# JOOBLE_API_KEY=
# JOB_SEARCH_INTERVAL_MINUTES=120
# JOB_SEARCH_MAX_CVS=3
